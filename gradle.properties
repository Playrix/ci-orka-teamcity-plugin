# Plugin version. Used by build.gradle to set the artifact version and to
# substitute the @Version@ token in teamcity-plugin.xml.
# Must match the pattern \d+\.\d+\.\d+.* (otherwise a SNAPSHOT-<timestamp> is used).
versionNumber=3.1.0-ci
